import { View, Text } from "react-native";
import React from "react";

const TextHeader = ({ text }) => {
  return <Text className="font-bold text-xl text-[#4465b8]">{text}</Text>;
};

export default TextHeader;
